{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pr-inbox.local/schemas/findings.schema.json",
  "title": "pr-inbox findings",
  "description": "Output of the dual-model-review agent, consumed by pr-inbox-web. v1.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "pr_url", "head_sha", "generated_at_utc", "findings"],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Schema version. Future versions bump this integer."
    },
    "pr_url": {
      "type": "string",
      "format": "uri",
      "description": "Canonical PR URL the review targets."
    },
    "pr_stable_identity": {
      "type": "string",
      "description": "Platform stable identity for rename-safe routing. Optional but recommended."
    },
    "head_sha": {
      "type": "string",
      "pattern": "^[0-9a-fA-F]{7,40}$",
      "description": "HEAD SHA the review was run against. Used for staleness checks before posting."
    },
    "base_sha": {
      "type": "string",
      "pattern": "^[0-9a-fA-F]{7,40}$"
    },
    "generated_at_utc": {
      "type": "string",
      "format": "date-time",
      "description": "Wall-clock time the file was written by the agent."
    },
    "models": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Deprecated combined model list retained for legacy schema-v1 documents. New producers use primary_models and shadow_models."
    },
    "primary_models": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "maxItems": 2,
      "uniqueItems": true,
      "description": "Primary reviewers whose independent reports determine findings and asymmetry. A complete review has exactly one Opus-family model and one GPT-family model."
    },
    "shadow_models": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "description": "Supplemental informational reviewers. These never count toward primary quorum or asymmetry."
    },
    "review_status": {
      "type": "string",
      "enum": ["complete", "degraded", "incomplete"],
      "description": "Whether the required primary review pair completed. Degraded mode must be explicitly approved by the operator."
    },
    "incomplete_reason": {
      "type": "string",
      "minLength": 1,
      "description": "Why the primary review is incomplete. Required when review_status is incomplete."
    },
    "asymmetry": {
      "type": "object",
      "additionalProperties": true,
      "description": "Diagnostic breakdown of agreement between primary reviewers only. Shadow reviewers never contribute. single_model is allowed only in explicitly approved degraded mode.",
      "properties": {
        "both_found": { "type": "integer", "minimum": 0 },
        "opus_only": { "type": "integer", "minimum": 0 },
        "gpt_only":  { "type": "integer", "minimum": 0 },
        "single_model": { "type": "integer", "minimum": 0 }
      }
    },
    "findings": {
      "type": "array",
      "items": { "$ref": "#/$defs/Finding" }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "review_status": { "const": "complete" } },
        "required": ["review_status"]
      },
      "then": {
        "required": ["primary_models", "asymmetry"],
        "properties": {
          "primary_models": {
            "minItems": 2,
            "maxItems": 2,
            "allOf": [
              { "contains": { "pattern": "(?:[Oo]pus|claude-opus)" } },
              { "contains": { "pattern": "(?:[Gg][Pp][Tt]|gpt-)" } }
            ]
          },
          "asymmetry": {
            "not": { "required": ["single_model"] }
          }
        }
      }
    },
    {
      "if": {
        "properties": { "review_status": { "const": "degraded" } },
        "required": ["review_status"]
      },
      "then": {
        "required": ["primary_models", "asymmetry"],
        "properties": {
          "primary_models": { "minItems": 1, "maxItems": 1 },
          "asymmetry": { "required": ["single_model"] }
        }
      }
    },
    {
      "if": {
        "properties": { "review_status": { "const": "incomplete" } },
        "required": ["review_status"]
      },
      "then": {
        "required": ["incomplete_reason"]
      }
    },
    {
      "if": {
        "properties": {
          "asymmetry": { "required": ["single_model"] }
        },
        "required": ["asymmetry"]
      },
      "then": {
        "properties": { "review_status": { "const": "degraded" } },
        "required": ["review_status"]
      }
    }
  ],
  "$defs": {
    "Finding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "severity", "confidence", "file", "title"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable per-document id (e.g. f01). Used by pr-inbox-web to track per-finding 'posted' state."
        },
        "severity": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low"]
        },
        "confidence": {
          "type": "string",
          "enum": ["high", "medium", "low"]
        },
        "found_by": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Which models found this. Order is informational; uniqueness is not enforced by schema."
        },
        "file": {
          "type": "string",
          "description": "Repo-relative file path."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "line_end": {
          "type": "integer",
          "minimum": 1
        },
        "diff_anchorable": {
          "type": "boolean",
          "default": false,
          "description": "True when the finding's (file, line) sits inside the diff and is safe to post inline."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "One-line summary; renders as the finding header in pr-inbox-web."
        },
        "body": {
          "type": "string",
          "description": "Multi-line markdown explanation. The body posted as the PR comment."
        },
        "suggested_inline": {
          "type": "string",
          "description": "Optional GitHub ```suggestion code block, including the fence."
        }
      }
    }
  }
}
